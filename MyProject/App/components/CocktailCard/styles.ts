import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginBottom: 10,
        backgroundColor: '#000000',
        borderRadius: 20,
        padding: 13,
        display: 'flex',
        flexDirection: 'row',
    },
    flexed: {
        flex: 1,
    },
});